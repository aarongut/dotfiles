#!/bin/bash
# Symlink every file in this repo into $HOME with GNU stow.
# Re-run after adding new files; existing links are left alone.
set -e
cd "$(dirname "$0")"
git pull origin master
stow --no-folding --restow --target="$HOME" --verbose=1 .
